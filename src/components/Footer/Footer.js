import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.footer `
  height: 100%;
  background-color: tomato;
`;

const Footer = () => (
  <Wrapper>Footer works!</Wrapper>
);

export default Footer;